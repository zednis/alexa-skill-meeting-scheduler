{
  "name": "alexa-skill-meeting-scheduler",
  "version": "1.0.0",
  "description": "Meeting scheduler skill",
  "scripts": {
    "test": "echo \"no test specified\" && exit 0"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.0",
    "moment": "^2.21.0"
  }
}
